[env]
SQLALCHEMY_DATABASE_URL = "sqlite:///./tutor_software.db"
AUTH_SECRET_KEY = "clave_secreta_tutor_software_2025"
ALGORITHM = "HS256"

[build]
command = "cd backend && pip install -r requirements.txt"

[run]
command = "cd backend && python app/main.py"
